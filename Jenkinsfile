pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'docker ps'
            }
           
    }
    stage('push') {
            steps {
                sh 'ls -l'
            }
           
    }
